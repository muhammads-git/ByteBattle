from app.services.room import create_room, join_room, start_room

# Step 1: create a room, using a real player_id you already have in the DB
# create_room(player_id=1)

# join_room(2,'MSXFHF')

# start_room(1,'MSXFHF')